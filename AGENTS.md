# pickless-monthly-report-harness 作業方針

このリポジトリは、ピクルス社の月次レポート作成支援MVPを扱うprivate前提の実装リポジトリです。

## 守る境界

- KPI集計・期間比較・候補抽出はPythonで決定的に処理する。
- LLMは検証済み事実だけを受け取り、解釈・コメント・確認事項の下書きを作る。
- 事実と提案を混ぜない。除外キーワードは候補までで、広告媒体を変更しない。
- 顧客向け確定版は、人間の明示承認があるまで生成しない。
- 顧客データ、認証情報、APIキーをコミットしない。fixtureは完全合成・匿名化する。
- 初期MVPはCSV/JSONを入力とし、BigQuery・GA4などの常時接続は後続フェーズとする。

## 実装方針

- Python 3.9以上の標準ライブラリだけで動かす。
- 同じ入力、プロファイル、コードから同じ事実集計を再生成できるようにする。
- 原本を変更せず、実行ごとの出力ディレクトリへハッシュ付きで成果物を保存する。
- 案件ごとのキャンペーン→商材分類と使用セクションをプロファイルに保存し、翌月再利用する。
